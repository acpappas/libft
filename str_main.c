#include <string.h>
#include <stdio.h>

/*test for strncpy
int main ()
{
   char src[4];
   char dest[12];

   strcpy(src, "The");
   ft_strncpy(dest, src, 10);

   printf("Final copied string : %s\n", dest);
   
   return(0);
}
*/
